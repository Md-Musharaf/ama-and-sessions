**Adhikya Edammala**
**Q:** What is the Temporal Dead Zone?
**A:** The Temporal Dead Zone is the period between entering a scope and declaring a `let` or `const` variable. Accessing the variable during this time causes a `ReferenceError`.

**Allanki VV Manikanta Sai**
**Q:** What is the difference between `map()` and `forEach()`?
**A:** `map()` creates and returns a new array after applying a function to each element. `forEach()` only executes the function and does not return a new array.

**Arpit Yadav**
**Q:** What is a `package.json` file?
**A:** `package.json` is a configuration file in a Node.js project. It contains project information, dependencies, scripts, version, and other settings.

**Boorle Sowmya Sri Lakshmi**
**Q:** What is the use of an exchange in RabbitMQ?
**A:** An exchange receives messages from producers and routes them to one or more queues based on routing rules.

**Naman Sharma**
**Q:** What is the default port of RabbitMQ?
**A:** RabbitMQ uses port `5672` for AMQP connections. The management dashboard usually runs on port `15672`.

**Nayunipatruni Harsha Vardhan**
**Q:** What is `JSON.stringify()`?
**A:** `JSON.stringify()` converts a JavaScript object, array, or value into a JSON-formatted string.

**Parlapalli Sulochana**
**Q:** What is the use of the `with` statement in file operations?
**A:** The `with` statement automatically manages the file resource. It closes the file after the operation, even if an error occurs.

**Rongala Vasu**
**Q:** What is the difference between `undefined` and “not defined”?
**A:** `undefined` means a variable has been declared but has no assigned value. “Not defined” means the variable has not been declared in the current scope.

**Rovinpal Udupi**
**Q:** What is the difference between `setTimeout()` and `setInterval()`?
**A:** `setTimeout()` executes a function once after a specified delay. `setInterval()` executes a function repeatedly at fixed time intervals.

**Vikas Mehta**
**Q:** What is the difference between shallow copy and deep copy?
**A:** A shallow copy creates a new object but shares references to nested objects. A deep copy creates completely independent copies of all nested values.

**Himaswi**
**Q:** What is a callback?
**A:** A callback is a function passed as an argument to another function. It is executed after a task is completed or when a specific event occurs.
